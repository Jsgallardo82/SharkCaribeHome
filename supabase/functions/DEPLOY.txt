# Despliegue Wompi
#
# Checkout (crear pago): ya NO usa Edge Function desde el browser.
# Ejecuta en SQL Editor:
#   supabase/wompi_attendee_payment.sql   (columnas)
#   supabase/wompi_checkout_rpc.sql       (RPC + secreto integrity)
#
# Webhook (Wompi → marcar pago): sí usa Edge Function
#   wompi-webhook  (Verify JWT = OFF)
#   URL en Wompi: https://<PROJECT_REF>.supabase.co/functions/v1/wompi-webhook
#
# Secrets Edge (solo webhook necesita events):
#   WOMPI_EVENTS_SECRET
#
# El secreto de integridad del Widget va en private.wompi_secrets (SQL).
#
# Correos de inscripción (asistente / patrocinador / expositor):
#   ver supabase/functions/NOTIFY_REGISTRATION.txt
#   función: notify-registration
#
# Ticket por correo al pagar + número de boleto + QR + check-in:
#   1) SQL: attendee_ticket_number.sql (si falta)
#   2) SQL: attendee_checkin.sql
#   3) ver supabase/functions/TICKET_EMAIL.txt
#   4) Redeploy: wompi-webhook + resend-ticket
#   Secrets Resend: RESEND_API_KEY, RESEND_FROM
